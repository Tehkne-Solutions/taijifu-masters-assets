name: Build First Playable Lot 01 (Historical v1)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-packager-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Historical packaging notice
        shell: bash
        run: |
          echo 'FIRST_PLAYABLE_LOT01_V1_PACKAGER=RETIRED'
          echo 'RELEASED_LIAN=assets-pack-01-v2.0.0'
          echo 'CANONICAL_BASELINE=45+44=89'
          echo 'SIGNATURE=Tehkné Solutions'

# Historical v1 packager retained only for audit provenance.
# PACK 01 v2.0.0 is the released Lian source; canonical PR validation is v3.
# Tehkné Solutions
